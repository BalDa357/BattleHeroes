using UnityEngine;
using System.Collections;

public class healthPoints : MonoBehaviour {

    public float tilt;
	
	// Update is called once per frame
	void Update () {
        //transform.Rotate (Vector3.left * Time.deltaTime * tilt);     
    }
}
